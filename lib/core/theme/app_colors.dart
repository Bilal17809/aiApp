import 'package:flutter/material.dart';

// Whites
const Color kWhite = Color(0xFFFFFFFF);

// Blacks
const Color kBlack = Color(0xFF000000);

// Grays
const Color greyColor = Color(0xff626262);

//green
const greenColor = Color(0xff00AB3F);

//red
const kRed = Color(0xffE1020A); // use

const kBlue = Color(0xff305DDD);

const skyColor = Color(0xFF569CD4);

//text color
const Color blackTextColor = Color(0xFF000000);
const Color textWhiteColor = Color(0xFFFFFFFF);
const Color textGreyColor = Color(0xff626262);

//app primary
const appBarBgColor = Colors.white;
const bgColor = Colors.white;

const Color greyBorderColor = Color(
  0xffADADAD,
); // use in home_page image shadow

// use below

const kMediumGreen1 = Color(0xFF81C784);
const kMediumGreen2 = Color(0xFF4CAF50);

const Color whiteShadowColor = Color.fromARGB(
  51,
  255,
  255,
  255,
); // 0.2 opacity // for elevated button
const Color buttonBlueColor = Color(0xFF42A5F5);
const BorderRadius defaultButtonBorderRadius = BorderRadius.all(
  Radius.circular(12),
);

const Color yellowButtonColor = Color(0xFFFDD835); // similar to yellow.shade700
